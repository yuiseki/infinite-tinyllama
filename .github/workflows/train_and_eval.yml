name: Train and Eval

on: push

jobs:
  train:
    runs-on: gpu-sv-010
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Echo for test
        run: echo "Hello, world!"
